# TwoDSurvival - Progress Tracker

## Completed Steps

| # | Task | Date | Notes |
|---|------|------|-------|
| 1 | Initial project setup | 2026-02-14 | Created Unreal Engine project with default structure |
| 2 | GitHub Actions setup | 2026-02-14 | Added Claude Code workflow for issue handling (no code review) |
| 3 | 2D side-view camera setup | 2026-02-14 | SpringArm + Camera on BaseCharacter, side-scroll view, plane-constrained movement, smooth camera lag |
| 4 | 2D movement with character rotation | 2026-02-14 | MoveRight() BlueprintCallable function — moves left/right in world space, rotates character to face movement direction via SetControlRotation |
| 5 | Interaction system | 2026-02-15 | IInteractable interface, EInteractionType enum, UInteractionComponent on BaseCharacter. Proximity detection via dynamic sphere. Hold timer with movement lock, progress 0→1. Input (E key) bound in C++ via EnhancedInput (IA_Interact). UMG progress bar uses Add to Viewport with Get Player Character binding. |
| 6 | Inventory system | 2026-02-20 | UInventoryComponent (C++) reusable on player + containers. UItemDefinition data asset. FInventorySlot struct. WBP_InventoryWidget dual-panel (player + container). WBP_InventorySlot with drag-and-drop and item stacking. Centralized RefreshAll() bound to OnInventoryChanged delegate. BP_Cabinet container actor. UseItem BlueprintNativeEvent (Consumable, applies HealthRestoreAmount). IA_ToggleInventory (Tab) bound in C++. |
| 7 | Inventory context menu | 2026-02-24 | Right-click on WBP_InventorySlot → WBP_ContextMenu spawned at cursor. Category-driven buttons: Consumable → Use, bCanBeEquipped → Equip. Focus-based dismissal (bIsFocusable + Set Keyboard Focus + On Focus Lost). Child buttons Is Focusable = false to prevent premature focus loss. Position via Get Mouse Position On Viewport + Set Position in Viewport (bRemoveDPIScale: false). |
| 8 | Weapon equip system | 2026-02-24 | AWeaponBase (C++) — StaticMeshComponent + BoxComponent hitbox (disabled by default), BaseDamage, SourceItemDef reference. WeaponActorClass (TSubclassOf) added to UItemDefinition. EquipItem_Implementation: spawns weapon actor, attaches to WeaponSocketName socket on character mesh, stores EquippedWeapon ref. UnequipWeapon_Implementation: destroys actor, clears ref. Context menu shows Unequip instead of Equip when slot item matches EquippedWeapon.SourceItemDef. |
| 9 | Interaction mid-air fix | 2026-02-24 | Replaced DisableMovement() with bMovementLocked bool on ABaseCharacter. MoveRight checks flag and returns early. Gravity and physics still apply during hold interactions — character no longer hangs in mid-air when interacting while airborne. |
| 10 | C++ max plan — programmatic input + widget C++ classes | 2026-02-25 | Removed all manual IA_HotbarSlot1-6/AllItemDefinitions UPROPERTYs. CreateInputActions() builds hotbar (1-6), scroll wheel, F5/F9 at runtime via NewObject. AssetRegistry auto-scans all UItemDefinition assets into ItemDefMap — no manual assignment needed. UBodyPartRowWidget, UHealthHUDWidget, UHotbarWidget created as C++ UUserWidget subclasses. HotbarWidget builds 6 slot borders/icons/key labels dynamically in NativeConstruct. ToggleHealthUI_Implementation creates/destroys health HUD, shows/hides mouse cursor. HotbarWidget created in BeginPlay and always visible. Build.cs updated with AssetRegistry module. |
| 11 | Health HUD widget | 2026-02-25 | WBP_HealthHUD (parent: UHealthHUDWidget) with 6x WBP_BodyPartRow rows. Each row has PartLabel (fixed MinDesiredWidth), HealthBar (Fill, color-coded green/yellow/red/grey), BrokenText overlaid on bar. TitleBar Border for drag-to-move. Drag uses delta-based approach with InitDragPosition() to sync viewport position from BaseCharacter. Mouse cursor shown on open, hidden on close. SetPositionInViewport on spawn fixes full-screen stretch issue. FInputModeGameAndUI on open so first click registers immediately. |
| 12 | Hotbar widget | 2026-02-25 | WBP_HotbarWidget (parent: UHotbarWidget). SlotContainer HorizontalBox — 6 slots built dynamically in C++ NativeConstruct (border + icon + key label). Active slot highlighted gold. Keys 1-6 and scroll wheel work via programmatic IMC. CreateInputActions() moved to SetupPlayerInputComponent to fix null binding issue. |
| 13 | Inventory system — complete | 2026-02-25 | WBP_ItemTooltip (parent: UItemTooltipWidget) — ItemNameText, DescriptionText, StatsText, IconImage. Tooltip shows on mouse enter, hides on mouse leave and right-click. Context menu: OnClosed event dispatcher clears ContextMenuInstance reference. Assign to Hotbar button added (Weapon/Consumable). Backpack test asset (DA_Backpack, BonusSlots=4). Tooltip/context menu ordering fixed — tooltip removed before context menu opens, Is In Viewport guard in On Focus Lost prevents double-removal on spam. |
| 14 | Combat system | 2026-03-01 | IDamageable interface (TakeMeleeDamage — BlueprintNativeEvent + BlueprintCallable). AWeaponBase: BeginAttack(mult) enables hitbox + SwingWindowDuration timer; EndAttack() public for AnimNotify. BeginAttack guard prevents double-call. AnimNotify_BeginAttack / AnimNotify_EndAttack C++ classes for frame-accurate hitbox open/close. ABaseCharacter implements IDamageable — damages EBodyPart::Body. Attack blocked when mouse cursor visible. HandlePlayerDeath() — sets bMovementLocked, plays DeathMontage, DisableInput, fires OnPlayerDied BlueprintImplementableEvent. |
| 15 | Enemy AI system | 2026-03-01 | AEnemyBase (C++, ACharacter + IDamageable) — C++ state machine (Idle/Patrol/Chase/Attack/Dead), no NavMesh or Behavior Trees. UHealthComponent reused (Body part only). UBoxComponent MeleeHitbox (disabled by default, enabled per swing). UWidgetComponent health bar (hidden until first hit). Manual X-axis movement via AddMovementInput, plane-constrained to match player. Aggro via sphere distance check (no line-of-sight). PostAttackStandStillDuration (EditDefaultsOnly) prevents movement/slide after swing. Rotation locked during bIsAttacking so player can jump over mid-attack. AWorldItem (AActor + IInteractable) — loot pickup actor, press E to add to inventory. UEnemyHealthBarWidget (UUserWidget) — ProgressBar BindWidget, color-coded green/yellow/red, attached via WidgetComponent. FLootEntry struct + LootTable on enemy for configurable drops. Blueprint steps: BP_EnemyBase (mesh, AnimBP, loot table, montages), WBP_EnemyHealthBar (ProgressBar layout), ABP_Enemy (blend space + DefaultSlot). |
| 16 | Bug fix — enemy death not triggering | 2026-03-02 | OnDeath delegate declared in UHealthComponent but never broadcast. Added broadcast in ApplyDamage when a vital part (Head or Body) transitions to broken. Also added Dead-state guard in TakeMeleeDamage after ApplyDamage and made Dead a terminal state in SetEnemyState. |
| 17 | Bug fix — interaction detection broken for Blueprint actors | 2026-03-02 | Cast<IInteractable>(OtherActor) only works for C++ interface implementations. Reverted to OtherActor->Implements<UInteractable>() which correctly detects both C++ and Blueprint interactables (e.g. BP_Cabinet). |
| 18 | Crafting system | 2026-03-02 | UCraftingRecipe (UDataAsset) — RecipeID, Ingredients (FIngredientEntry array), OutputItemID, OutputCount. UCraftingComponent (C++) on BaseCharacter — auto-scans all recipes + item defs via AssetRegistry, CanCraft/TryCraft, FindItemDef. UCraftingWidget (C++ UUserWidget) — left ScrollBox recipe list, right detail panel (output icon/name/count, ingredient rows with have/need counts color-coded, craft button, status text). UCraftingRecipeEntry (C++ UUserWidget) — single recipe row, fires OnRecipeEntryClicked delegate. ACraftingTable (AActor + IInteractable) — UBoxComponent InteractionBox, press E to open/close crafting UI, OnBoxEndOverlap auto-closes widget when player walks away. CloseCrafting() BlueprintNativeEvent on BaseCharacter for unconditional close. CountItemByID + RemoveItemByID added to UInventoryComponent. All interactable actors now use UBoxComponent (not sphere) for proximity detection. |
| 19 | UI cursor + inventory bug fixes | 2026-03-02 | Reference-counted cursor: ShowUICursor()/HideUICursor() on ABaseCharacter — cursor only hides when ALL open UIs have closed (fixes crafting+inventory cursor conflict). RegisterTooltip()/ClearTooltip() on ABaseCharacter — tracks active WBP_ItemTooltip in C++; ClearTooltip() force-removes it when any panel closes, fixing stuck tooltip on container close. WBP_InventorySlot OwnerComp fix — OnMouseEnter now reads item def from OwnerComp (set per-slot at creation time) instead of always going to BaseCharacter.InventoryComponent, fixing container slots showing player items. Tab key (ToggleInventory) now always hides ContainerUI on open, and calls CloseContainerInventory before close if container was visible. |
| 20 | Time system — day/night cycle | 2026-03-04 | ATimeManager (C++ AActor) — tick-driven day/night cycle, no curve assets. GetSolarElevation() computes 0–1 sin arc between SunriseTime and SunsetTime. ApplyVisuals() drives DirectionalLight intensity+color, SkyLight intensity, ExponentialHeightFog density, PostProcess scene tint. All colors/intensities are EditAnywhere C++ defaults — no editor curve assets needed. GetSunColor() piecewise lerp: SunriseColor→NoonColor→SunsetColor with SmoothStep. SetDaylight(sunrise, sunset) BlueprintCallable for climate system to shift day length at runtime. Components found at BeginPlay via GetComponentByClass — BP_TimeManager adds Directional Light / Sky Light / Exponential Height Fog / Post Process freely with any name. FOnDayPhaseChanged delegate broadcast on day↔night crossing for enemy spawner. |
| 21 | Needs system — Hunger / Thirst / Fatigue | 2026-03-04 | UNeedsComponent (C++) — tick-driven drain for Hunger/Thirst/Fatigue (0–100), FOnNeedChanged delegate. ActiveMultiplier doubles drain when running/attacking. At 0: HP drain on EBodyPart::Body. Below WarningThreshold (50): speed –30%, damage –20%, health regen blocked. UNeedsWarningWidget (C++ UUserWidget) — right-side HUD, 3 vertical progress bars built dynamically via WidgetTree, collapse when need ≥ 50%. ABedActor (AActor + IInteractable) — press E to sleep/wake; SleepStartFatigue guard prevents immediate auto-wake when fatigue is already near full; wake-up via E press or fatigue reaching 100 (if started below 95%). StartSleeping/StopSleeping BlueprintNativeEvent on BaseCharacter: SetIgnoreMoveInput + MOVE_None + per-tick StopMovementImmediately() for Blueprint input bypass. ATimeManager::TimeScale scales day speed 20× during sleep; NeedsComponent::SleepTimeScaleMultiplier matches fatigue restore rate. HungerRestore/ThirstRestore/FatigueRestore added to UItemDefinition for consumables. Needs saved/loaded via UTwoDSurvivalSaveGame. Weapon damage multiplied by NeedsComponent::GetDamageMultiplier(). |
| 22 | Street light actor | 2026-03-04 | AStreetLight (C++ AActor) — UStaticMeshComponent + UPointLightComponent. Binds to ATimeManager::OnDayPhaseChanged at BeginPlay. SetLightOn: toggles visibility + intensity. Reads TM->IsNight() on spawn for correct initial state. LightIntensity and LightColor are EditAnywhere — tweak per-light in Details panel. Blueprint child BP_StreetLight: assign mesh, position Light component. |
| 23 | World map — seamless street streaming | 2026-03-05 | UStreetDefinition (UDataAsset) — StreetID, StreetName, StreetWidth, TSoftObjectPtr<UWorld> Level, ExitLeft/Right/Up (direct references to adjacent streets), GetExit(Direction). EExitDirection enum (Left/Right/Up). UStreetManager (UGameInstanceSubsystem) — seamless streaming via ULevelStreamingDynamic::LoadLevelInstanceBySoftObjectPtr; InitializeWithStreet() loads the starting street; OnPlayerCrossedExit() computes adjacent offset and loads next street; OnNewStreetShown() UFUNCTION bound to OnLevelShown delegate — unloads previous street and commits new current street; bTransitionInProgress guard prevents overlapping transitions; OnStreetChanged multicast delegate notifies HUD. AStreetExit (C++ AActor) — TriggerBox BoxComponent at the Left/Right boundary; OnTriggerOverlap → UStreetManager::OnPlayerCrossedExit; no IInteractable, no E-press needed. AStreetBootstrapper (C++ AActor) — place in persistent level; StartingStreet + StartingOffset properties; BeginPlay calls InitializeWithStreet. UStreetHUDWidget (C++ UUserWidget) — ArrowLeft/Right/Up BindWidget Images; NativeConstruct binds OnStreetChanged delegate; RefreshArrows reads CurrentStreet exits directly from UStreetManager (no actor scanning). No save/load on transition — player state persists naturally in the persistent level. Blueprint steps: WBP_StreetHUD (3 Images), assign StreetHUDWidgetClass on BP_BaseCharacter, create BP_StreetBootstrapper (place in persistent level), create DA_Street_* data assets with ExitLeft/Right/Up wired up, create BP_StreetExit (place at level boundaries). |
| 24 | Building system — C++ procedural room generation | 2026-03-05 | UBuildingDefinition (UDataAsset) — BuildingType (House/Hospital/Store), FloorCount, RoomsPerFloor, RoomWidth, FloorHeight, StairX, bHasElevator, ElevatorX, RoomActorClass, StairsActorClass, ElevatorRoomActorClass. ABuildingGenerator (C++ AActor) — BeginPlay calls Generate(); UFUNCTION(CallInEditor) Generate() button in Details panel for in-editor preview without Play; spawns RoomActorClass/StairsActorClass/ElevatorRoomActorClass in a FloorCount×RoomsPerFloor grid; StairX/ElevatorX matched via FMath::IsNearlyEqual; editor-spawned actors marked RF_Transient so they don't save into the level. ABuildingEntrance (C++ AActor + IInteractable) — press E to enter/exit building; prompt reads SM->bIsInsideBuilding for correct label; calls SM->OnPlayerCrossedExit(Up). UStreetManager extended — Up direction now enters/exits buildings; saves ReturnStreet/Offset/PlayerLocation on enter; restores on exit; teleports player to ABuildingEntrance inside on entry, ReturnPlayerLocation on exit; bIsInsideBuilding flag. UStreetDefinition extended — bIsPCGBuilding bool, BuildingDefinition (EditCondition). Buildings stream in at X=100000 (separate from street grid). Blueprint steps: create BP_RoomCell/BP_StairSet/BP_ElevatorRoom actors, DA_Building_* data assets, building sublevels with BP_BuildingGenerator + BP_BuildingEntrance, DA_Street_* with ExitUp wired. |
